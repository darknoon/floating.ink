{
  "name": "floating-ink-backend",
  "version": "0.0.1",
  "description": "GraphQL backend for app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "monitor": "nodemon --exec npm run babel-node -- server.js",
    "babel-node": "babel-node server.js --presets es2015,stage-2 --ignore='node_modules'",
    "deploy": "gcloud app deploy app.yaml",
    "build": ""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "express-graphql": "^0.6.1",
    "graphql": "^0.8.2"
  },
  "engines": {
    "node": ">=4.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "nodemon": "^1.11.0"
  }
}
